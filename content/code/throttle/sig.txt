Throttled&lt;T&gt; throttle&lt;T&gt;(void Function(T arg) func, Duration wait, {bool leading = true, bool trailing = true})

class Throttled&lt;T&gt; {
  void call(T arg);   // Throttled is callable: throttled(arg)
  void cancel();
}

FxEvents&lt;T&gt; FxEvents&lt;T&gt;.throttle(Duration window, {bool leading = true, bool trailing = false}) // chain (events)

Throttled&lt;T&gt; (void Function(T arg)).fxThrottle(Duration wait, {bool leading = true, bool trailing = true}) // method
