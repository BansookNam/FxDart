import 'package:fxdart/fxdart.dart';

void main() {
  final pairs = [('id', 101), ('active', 1)];

  // TODO: use fromEntries to turn `pairs` into a Map
  final Map<String, int> record = {};

  print(record);
}
