import 'package:fxdart/fxdart.dart';

void main() {
  final order = {'id': 42, 'status': 'shipped', 'total': 99.5};

  // TODO: use isMatch to check whether `order` matches {'status': 'shipped'}
  final isShipped = false;

  print(isShipped);
}
